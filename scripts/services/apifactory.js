(function() {
    'use strict';

    angular.module('contractor').factory('apiFactory', apiFactory);

    apiFactory.$inject = ['Restangular', 'API_URL', 'API_ANALYTICS_VERSION'];

    function apiFactory(Restangular, API_URL, API_ANALYTICS_VERSION) {
        
    }

})();
